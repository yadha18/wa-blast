# Leave empty for local dev (Vite proxy handles /api and /socket.io).
# For production, set to your deployed backend's public URL, no trailing slash:
# VITE_BACKEND_URL=https://wa-blast-server-production.up.railway.app
VITE_BACKEND_URL=
